// Configuration Firebase du projet.
//
// Remplace les valeurs ci-dessous par celles de TON projet Firebase
// (Console Firebase > Paramètres du projet > Tes applications > SDK setup
// and configuration). Voir le README pour la procédure complète.
//
// Ces valeurs ne sont pas secrètes : elles identifient publiquement ton
// projet Firebase. Ce qui protège vraiment les données, ce sont les règles
// de sécurité Firestore (voir firestore.rules et le README).

export const firebaseConfig = {
  apiKey: "REMPLACE_MOI",
  authDomain: "REMPLACE_MOI.firebaseapp.com",
  projectId: "REMPLACE_MOI",
  storageBucket: "REMPLACE_MOI.appspot.com",
  messagingSenderId: "REMPLACE_MOI",
  appId: "REMPLACE_MOI",
};
